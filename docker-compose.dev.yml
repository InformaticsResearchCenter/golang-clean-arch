version: "3.9"
services:
  iteung-android-api-golang-dev:
    environment:
      - TZ=Asia/Jakarta
      - APP_ENV=dev
      - DB_USERNAME_SIAP=irc
      - DB_PASSWORD_SIAP=rollyganteng
      - DB_HOST_SIAP=10.14.200.17
      - DB_PORT_SIAP=3306
    image: $CI_REGISTRY_IMAGE:latest
    container_name: iteung-android-api-golang-dev
    restart: unless-stopped
    expose:
      - "8080"
    networks:
      iteungwebnet:
        ipv4_address: 172.18.0.56

networks:
  iteungwebnet:
    external:
      name: iteungwebnet
